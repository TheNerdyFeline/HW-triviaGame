#EDE01C yellow in chandelier

first attaempt to add choice buttons
for (var  i = 0; i < answerButtons.length; i++) {
    var choices = $("<button>");
    choices.addClass("answerChoices");
    choices.text(answerButtons[i]);
    $(".multiChoice").append(choices);
    if (answerButtons[i] === randomQuest.correct) {
    choices.addClass("correct");
}	
console.log(randomQuest.correct);
};

$(".answerChoices").after($("<br>"));

2nd attempt to add choice buttons
for (var  i = 0; i < answerButtons.length; i++) {
var choices = $("<button>");
choices.addClass("answerChoices");
choices.text(answerButtons[i]);
}
(choices).html($("multiChoice"));
$("button").after($("<br>"));

// trying to get click function to work
 if ($(".answerChoices").hasClass("notRight"))
